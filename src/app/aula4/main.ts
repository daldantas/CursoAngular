  // JS Only
  var numbersJS = [1, 2, 3];
  numbersJS.map(function(val){return val * 2;})

  // ES6(2015) TS
  let numbersTS = [1, 2, 3];
  numbersTS.map(val => val * 2);

  // Class declaration ES6(2015) TS
class Mathematic {
    soma2(x, y){
        return x + y;
    }
}